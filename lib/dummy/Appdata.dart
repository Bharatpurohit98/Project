/*import 'package:flutter/material.dart';

class Appdata extends StatefulWidget {
  const Appdata({ Key? key }) : super(key: key);

  @override
  _AppdataState createState() => _AppdataState();
}

class _AppdataState extends State<Appdata> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page'),
      ),
      floatingActionButton: FloatingActionButton(onPressed: Navigator.pop(context,),child: Icon(Icons.add,color: Colors.teal,),),
    );
  }
}*/